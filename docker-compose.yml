version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: mysql-1
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: task_db
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-proot"]
      interval: 5s
      timeout: 5s
      retries: 10


  app:
    build: .
    container_name: task-management-app
    restart: always
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      DB_USERNAME: root
      DB_PASSWORD: root
      MAIL_USERNAME: tasks422@gmail.com
      MAIL_PASSWORD: ukvy_hnrp_vfqn_bcbn
      JWT_SECRET: hQw9RZP0f4x2YJY6vPpZx8mQ3cZ7V9WcJxkM0S2uF5A=

volumes:
  mysql_data:
